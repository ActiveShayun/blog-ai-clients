'use client'
import React from 'react';

const Title = ({ title }) => {
    return (
        <div>
            <h2 className='text-2xl font-bold mb-2'>{title}...</h2>
        </div>
    );
};

export default Title;