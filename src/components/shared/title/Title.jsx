'use client'
import React from 'react';

const Title = ({ title }) => {
    return (
        <div>
            <h2 className='text-xl font-semibold mb-2'>{title}...</h2>
        </div>
    );
};

export default Title;